import { WhoIsUserPipe } from './who-is-user.pipe';

describe('WhoIsUserPipe', () => {
  it('create an instance', () => {
    const pipe = new WhoIsUserPipe();
    expect(pipe).toBeTruthy();
  });
});
